brew install homebrew/cask-fonts/font-meslo-lg-nerd-font
brew install fzf gh go helm jq kubectx kubernetes-cli kubie stern terraform tmux yadm zsh
brew install equinix/tap/metal-cli
brew install goreleaser/tap/goreleaser
#brew install 1password
#brew install adobe-acrobat-reader
#brew install discord
#brew install firefox
#brew install github
#brew install iterm2
#brew install microsoft-edge
#brew install microsoft-teams
#brew install homebrew/cask/onedrive
#brew install pingid
#brew install pritunl
#brew install slack
#brew install telegram
#brew install zoom
